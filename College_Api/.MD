Assignment 1: Student Management API
Follow these steps.
1. Initialize a Node.js project using npm init
2. Install Express and Mongoose
3. Create an server.js file to set up the server
4. Use Mongoose to establish a connection with MongoDB
5. Create an student.js file to store the database configuration
6. Define a student schema with fields id, name, age, course, email
7. Create routes.js for setting up the routes
8. Implement an endpoint (POST /students) to create and save a new student to the
database
9. Validate required fields before saving
10.Implement an endpoint (GET /students) to fetch all students from the database
11.Implement an endpoint (GET /students/:id) to fetch a specific student by their id
12.Handle errors for invalid or non-existing id
13.Implement an endpoint (PUT /students/:id) to update student’s details
14.Implement an endpoint 

///

id- required, unique

name- Required, must be at least 3 characters

age- Required, Must be a number between 18 and 30

course- Required, Must be either "FY MCA", "SY MCA", “FY MBA” or "SY MBA"

email- Required, Valid email id having minimum two domain segments
